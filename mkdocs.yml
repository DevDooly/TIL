site_name: My TIL Wiki
site_url: http://devdooly.iptime.org:8000
repo_url: https://github.com/devdooly/til  # 👈 본인의 저장소 주소로 꼭 변경하세요!
edit_uri: edit/main/docs/                # GitHub 웹 편집 버튼이 docs 폴더를 향하도록 설정

# 📌 핵심: 설정 파일 위치(wiki) 기준, 상위 폴더의 docs를 문서 소스로 지정

theme:
  name: material
  language: ko
  palette: 
    # 라이트 모드 (기본)
    - media: "(prefers-color-scheme: light)"
      scheme: default
      primary: indigo
      accent: indigo
      toggle:
        icon: material/weather-sunny
        name: 다크 모드로 전환
    # 다크 모드
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      primary: indigo
      accent: indigo
      toggle:
        icon: material/weather-night
        name: 라이트 모드로 전환
  features:
    - navigation.expand     # 메뉴 펼침 유지 (Wiki 스타일)
    - navigation.indexes    # 폴더 클릭 시 index 페이지로 이동
    - navigation.top        # 맨 위로 가기 버튼
    - navigation.tracking   # 스크롤 위치 추적
    - search.suggest        # 검색어 자동 완성
    - search.highlight      # 검색 결과 강조
    - content.code.copy     # 코드 복사 버튼
    - toc.follow            # 우측 목차 스크롤 추적

nav:
  - Home: index.md
  - Language: Language/
  - Web: Web/
  - Infrastructure: Infrastructure/
  - Data: Data/
  - Computer Science: ComputerScience/
  - Tools: Tools/
  - Life: Life/
  - Archive: Archive/

markdown_extensions:
  - admonition
  - pymdownx.details
  - pymdownx.superfences
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.tabbed:
      alternate_style: true
  - toc:
      permalink: true

plugins:
  - search

